spring:
  application:
    name: Spring-Batch-With-S3

  datasource:
    url: jdbc:mysql://localhost:3306/rojkharido
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

  s3:
    accessKey:
    secretKey:
    bucket: rojkharido-bucket